services:
  rag-agent:
    build: .
    container_name: rag-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./chroma_db:/app/chroma_db
      - ./agent_logs.jsonl:/app/agent_logs.jsonl
    restart: unless-stopped